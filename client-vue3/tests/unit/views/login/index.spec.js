import Login from "@/views/login/index.vue";

import { mount } from "@vue/test-utils";

test("测试登录页面随机渐变背景", () => {
  // given
  // const wrapper = mount(Login, {
  //   shallow: true,
  // });
  // console.info(wrapper.vm);
  // const bgColor = wrapper.vm.bgColor;

  // when
  // const loginPage = wrapper.find('.login-page');

  // then
});
